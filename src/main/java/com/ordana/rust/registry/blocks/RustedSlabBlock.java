package com.ordana.rust.registry.blocks;

public class RustedSlabBlock extends WeatheredSlabBlock{
    public RustedSlabBlock(RustLevel rustLevel, Settings settings) {
        super(rustLevel, settings);
    }
}

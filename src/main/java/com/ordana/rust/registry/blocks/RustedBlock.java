package com.ordana.rust.registry.blocks;

public class RustedBlock extends WeatheredBlock{
    public RustedBlock(RustLevel rustLevel, Settings settings) {
        super(rustLevel, settings);
    }
}
